# Performance Estimate — ZSAP_FI086A

## 主驱动表

| 表名 | 说明 | 典型数据量（单公司+单年） |
|------|------|--------------------------|
| FAGLFLEXT | 总账：总计表 | 5,000 ~ 50,000 行 |

## 数据量级分类

- **单公司代码 + 单年度 + 全期间 + 全科目**：预估 < 100,000 行
- **增加期间/科目筛选后**：预估 < 50,000 行
- **结论**：在线 ALV 全量输出可行，无需强制分页

## 索引分析

FAGLFLEXT 主键包含：`RCLNT`, `RYEAR`, `OBJNR00~08`, `RBUKRS`, `RACCT`, `RPMAX`, `DRCRK`, `RTCUR`, `PRCTR`, `RFAREA` 等。

- **WHERE 命中字段**：`RBUKRS`, `RYEAR`, `RACCT`, `RPMAX`
- **索引建议**：上述字段均为键字段或前导键字段，查询应走主键索引，无全表扫描风险

## 分页建议

| 场景 | 建议 |
|------|------|
| 在线 ALV | 直接全量展示，无需分页 |
| 后台作业 | 若用户选择全公司+多年度，建议 `SUBMIT` 后台执行 |

## 探针记录

| 探针 | 结果 | 备注 |
|------|------|------|
| RFC_READ_TABLE FAGLFLEXT | 失败 (AD 718) | 簇表/权限限制，非数据量问题 |
| RFC_READ_TABLE SKA1 | 5 行 | 表可访问 |
| RFC_READ_TABLE ZSAP_BUKRS | 0 行 | 当前客户端可能无配置数据 |
