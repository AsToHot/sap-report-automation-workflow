# ZSAP_FI244 拉取清单

- 包：`ZABAP`
- 对象：`ZSAP_FI244`
- 时间：`2026-04-21`
- MCP 服务器：`project-0-ABAP工作流-user-ai-abap`

## 已落盘

- `ZSAP_FI244.abap`（主程序）
- `ZSAP_FI244T01.abap`（Include）
- `ZSAP_FI244SEL.abap`（Include）
- `ZSAP_FI244F01.abap`（Include）
- `ZSAP_FI244.progpx.xml`（PROG/PX 元数据）

## GUI 状态 / 文本元素

- 已通过 `objectStructure` 确认程序对象存在 `Text Elements` 关联。
- 通过当前 `getObjectSource` 可读到 `PROG/P` 与 `PROG/PX` 源/元数据；但 `Text Elements` 与 `GUI Status` 在该系统返回的 ADT 端点未暴露可直接读取的 source URL（直接请求返回 404）。
- 当前目录已完整保存可读取的报表对象源码与程序元数据。
